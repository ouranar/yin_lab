# 内容维护说明

## 1. 现在这套 Vue 项目怎么维护

这套站点已经改成纯前端静态方式运行。

也就是说：

- 没有后台登录后写入内容的流程
- 没有数据库
- 没有服务端接口
- 修改内容的方式，就是直接改本地文件

这样的好处是结构简单、部署轻、后续迁移方便。

## 2. 平时主要改哪些文件

### `content/site-content.json`

这是最核心的内容文件。

适合维护：

- 首页内容
- 关于页内容
- 新闻
- 成员
- 研究内容
- 出版物
- 招募
- 联系方式

如果你只想先维护中文版，优先改这个文件即可。

### `src/lib/localized-site-data.ts`

这是多语言覆盖文件。

适合维护：

- 英文页面内容
- 日文页面内容
- 某些页面在不同语言下的单独表达

建议理解方式是：

- `site-content.json` 负责基础内容
- `localized-site-data.ts` 负责英文和日文的覆盖版本

### `src/lib/i18n.ts`

这是界面文案文件。

适合维护：

- 导航名称
- 按钮文字
- 页面标题
- 页脚文案
- 一些固定提示语

如果你发现“页面结构内容已经换了，但按钮还是旧文字”，通常就要改这里。

### `src/styles/globals.css`

这是全局样式文件。

适合维护：

- 页面整体留白
- 卡片样式
- 列表、按钮、导航、页脚等通用视觉风格

如果你想改站点整体风格，而不是只改某一页的内容，通常会动到这里。

### `public/uploads/`

这是图片目录。

维护方式：

1. 把图片放进这个目录
2. 在内容文件里写图片地址，例如：

```text
/uploads/example.jpg
```

## 3. 推荐维护流程

每次更新内容，建议按这个顺序来：

1. 先改 `content/site-content.json`
2. 如果英文或日文也要同步，再改 `src/lib/localized-site-data.ts`
3. 如果导航、按钮、固定标题要调整，再改 `src/lib/i18n.ts`
4. 如果有新图片，放进 `public/uploads/`
5. 本地运行检查
6. 构建确认无报错
7. 提交并推送到 git

## 4. 本地检查命令

```powershell
npm.cmd run dev
```

构建检查：

```powershell
npm.cmd run build
```

## 5. 这种方式和 Markdown 比，为什么更适合你现在这个项目

你前面已经明确提出：

- 不要后端交互
- 希望结构稳定
- 后续还要做中英日三语

在这种情况下，先用 `JSON + 本地图片` 会比 `Markdown` 更直接：

- 结构化更强，适合页面卡片、列表、分组、按钮链接这些内容
- 多语言更容易做字段级对应
- 不需要额外解析 Markdown 样式
- 对招募、成员、出版物、联系方式这类固定结构页面更友好

Markdown 并不是不能用，但它更适合长文章。
你这个项目大量内容属于“页面区块 + 列表 + 字段”，所以当前阶段保留 `JSON` 更合适。

## 6. 后面如果内容越来越多，建议再做一次拆分

当前这版已经可以维护，但如果后面内容继续增长，我建议下一步做这个优化：

- 把 `site-content.json` 按页面拆分
- 把多语言覆盖按语言拆分

例如未来可以变成：

- `content/zh/home.json`
- `content/zh/about.json`
- `content/zh/publications.json`
- `content/en/home.json`
- `content/ja/home.json`

这样会比一个超大的总文件更好维护。

## 7. 你以后如果只想“改文字”，最简方式是什么

最简方式就是：

1. 改 `content/site-content.json`
2. 放图片到 `public/uploads/`
3. 运行 `npm.cmd run dev` 看效果

如果你愿意，我下一步可以继续帮你做一件事：
把当前的大 `site-content.json` 再拆成“按页面分文件”的 Vue 内容结构，这样后续维护会更轻松。
